services:
  - type: web
    name: nutition-tracker
    runtime: node
    buildCommand: npm ci
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
